import 'package:flutter/material.dart';

class PageViewZoomTransition extends StatefulWidget {
  const PageViewZoomTransition({Key? key}) : super(key: key);

  @override
  State<PageViewZoomTransition> createState() => _PageViewZoomTransitionState();
}

class _PageViewZoomTransitionState extends State<PageViewZoomTransition> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
